@model GameStoreMVC.Models.ProductoModel

@{
    ViewData["Title"] = "Editar Producto";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-warning text-dark text-center py-3">
                    <h4 class="mb-0 fw-bold">EDITAR PRODUCTO</h4>
                </div>
                <div class="card-body p-4">
                    <form asp-action="Edit" method="post">

                        <input type="hidden" asp-for="IdProducto" />

                        <div class="mb-3">
                            <label class="form-label fw-bold">Nombre del Videojuego</label>
                            <input asp-for="Nombre" class="form-control" required />
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Descripción</label>
                            <textarea asp-for="Descripcion" class="form-control" rows="2"></textarea>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label class="form-label fw-bold">Precio (S/)</label>
                                <input asp-for="Precio" type="number" step="0.01" class="form-control" required />
                            </div>
                            <div class="col-md-6 mb-3">
                                <label class="form-label fw-bold">Stock</label>
                                <input asp-for="Stock" type="number" class="form-control" required />
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label class="form-label fw-bold">Formato</label>
                                <select asp-for="Formato" class="form-select">
                                    <option value="Físico">Físico (Caja)</option>
                                    <option value="Digital">Digital (Código)</option>
                                </select>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label class="form-label fw-bold">Edición</label>
                                <select asp-for="Edicion" class="form-select">
                                    <option value="Estándar">Estándar</option>
                                    <option value="Deluxe">Deluxe / Especial</option>
                                    <option value="Coleccionista">Coleccionista</option>
                                    <option value="GOTY">GOTY (Game of the Year)</option>
                                </select>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-bold">URL Imagen</label>
                            <input asp-for="ImagenUrl" class="form-control" />
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-bold">Categoría</label>
                            <select asp-for="IdCategoria" class="form-select">
                                <option value="">-- Seleccione una opción --</option>
                                <option value="1">1 - Juegos PS5</option>
                                <option value="2">2 - Juegos Switch</option>
                                <option value="3">3 - Consolas</option>
                                <option value="4">4 - Accesorios</option>
                            </select>
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-action="Index" class="btn btn-outline-secondary me-md-2">Cancelar</a>
                            <button type="submit" class="btn btn-warning px-5">Guardar Cambios</button>
                        </div>

                    </form>
                </div>
            </div>
        </div>
    </div>
</div>